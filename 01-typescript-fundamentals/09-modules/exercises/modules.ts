// modules.ts — Run with: npx tsx exercises/modules.ts
//
// TODO: Import what you need from link-helpers.ts and link-types.ts
// Then:
// 1. Create an array of ProfileLink objects (at least 4, mix of categories)
// 2. Filter to only visible links with valid URLs
// 3. Group them by category using Record<string, ProfileLink[]>
// 4. Print each category and its links using formatLinkDisplay
